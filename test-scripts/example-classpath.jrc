# Example Classpath Configuration
# This shows how to use -cp instead of -jar
# Useful for non-fat-jar applications

# VM arguments
vm.args=-Xmx256m

# Java arguments with classpath
# Windows uses semicolon separator, Linux uses colon
java.args=-cp lib/*;app.jar com.example.Main

# Application arguments
app.args=--config config.xml

# AOT disabled for classpath mode (optional)
aot=false
